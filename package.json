{
  "name": "felix-vault",
  "version": "0.1.0",
  "private": true,
  "description": "Felix delta-neutral carry-trade vault prototype (Hyperliquid testnet)",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run --network hyperEvmTestnet scripts/deploy.hardhat.js",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
