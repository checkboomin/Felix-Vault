"""Felix carry-trade operator bot package.

Run the operator with:   python -m bot.operator   (from the repo root)

Note: this module is named `bot.operator`. It must be imported as part of the
`bot` package (never with `bot/` itself on sys.path[0]) so it does not shadow
Python's stdlib `operator` module that dependencies rely on.
"""
